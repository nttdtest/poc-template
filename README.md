# Template

This template consumes from the skeleton and adds Terraform files that are useful to the agents setting up the primitives, but don't necessarily need their changes automatically pushed across our ecosystem.

